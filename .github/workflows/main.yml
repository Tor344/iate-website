name: Deploy to Digital Ocean
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions

      - name: Install
        run: nmp install

      - name: Run Typecheck
        run: npm run typecheck
